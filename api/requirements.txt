# Dependencies for the Vercel function. Kept next to the entrypoint on purpose:
# the repo root's requirements.txt belongs to the Windows widget and must not be
# what gets installed on a Linux build.
flask>=3.0
sqlalchemy>=2.0
psycopg[binary]>=3.1
authlib>=1.3
argon2-cffi>=23.1
itsdangerous>=2.2
requests>=2.31
pillow>=10.0
